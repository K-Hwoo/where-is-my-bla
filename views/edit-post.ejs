<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("includes/head.ejs") %>
    <link rel="stylesheet" href="/styles/team-build.css" />
  </head>
  <body>
    <%- include("includes/header.ejs") %>

    <main>
      <section>
        <h1>Edit Your Post</h1>
        <form action="/post/<%= post._id %>/edit " method="POST">
          <div class="form-control">
            <label for="title">Post Title</label>
            <input
              type="text"
              id="title"
              name="title"
              value="<%= post.title %>"
              required
            />
          </div>

          <div class="form-control">
            <label for="summary">Post Summary</label>
            <input
              type="text"
              id="summary"
              name="summary"
              required
              maxlength="255"
              value="<%= post.summary %>"
            />
          </div>

          <div class="form-control">
            <label for="content">Post Content</label>
            <textarea id="content" name="content" required rows="5">
<%= post.body %></textarea
            >
          </div>

          <div class="form-control">
            <label for="members">Members</label>
            <input
              type="number"
              id="members"
              name="members"
              value="<%= post.memNum %>"
              required
            />
          </div>

          <input type="file" />

          <button class="btn">Add Post</button>
        </form>
      </section>
    </main>
  </body>
</html>
